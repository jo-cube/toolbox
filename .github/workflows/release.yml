name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - cli: hello
            goos: darwin
            goarch: amd64
          - cli: hello
            goos: darwin
            goarch: arm64
          - cli: ksetoff
            goos: darwin
            goarch: amd64
          - cli: ksetoff
            goos: darwin
            goarch: arm64
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build archive
        env:
          CLI: ${{ matrix.cli }}
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          VERSION: ${{ github.ref_name }}
        run: |
          mkdir -p dist
          go build -ldflags "-s -w -X github.com/jo-cube/toolbox/internal/buildinfo.version=${VERSION}" -o "dist/${CLI}" ./cmd/${CLI}
          tar -czf "dist/${CLI}_${GOOS}_${GOARCH}.tar.gz" -C dist ${CLI}
          rm dist/${CLI}

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/${{ matrix.cli }}_${{ matrix.goos }}_${{ matrix.goarch }}.tar.gz
